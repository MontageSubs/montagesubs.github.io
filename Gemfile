source "https://rubygems.org"

# GitHub Pages bundle (includes Jekyll + allowed plugins like feed/seo-tag)
gem "github-pages", group: :jekyll_plugins

# For local `bundle exec jekyll serve` on Ruby 3+
gem "webrick"
